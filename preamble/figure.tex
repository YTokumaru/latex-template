% 画像を表示するためのパッケージ
\usepackage{float}          % 画像を強制的に指定位置に表示する[H]
\usepackage{svg}            % svg
\usepackage{graphicx}       % png, jpg pdf
\usepackage{caption}
\usepackage{subcaption}     % 画像を並べて表示する

% 表を表示するためのパッケージ
\usepackage{booktabs}